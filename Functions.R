# Functions

# Providing random values with mean 4 and sd as 0.4
aneruth <- rnorm(4,2,0.5) # or rnorm(n=4,mean=value,sd=value)
print(aneruth)

is.character('a')

a <- -1
typeof(a)

sqrt(123)

?paste(a)
