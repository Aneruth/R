func1 <- c(1,2,3,4)  # Combine Function
func2 <- seq(1,15,2) # Sequence function with start_val,stop_val,step_val
func3 <- rep('An',2) # Repeat an object

# Access an individually elements in a vector
vec <- c(1,2,3,4,5,6,7,8,9)

# To access the first element 
vec[1]

# To fetch all the elements except for the first one
vec[-1]
vec[c(2,4,6)]
vec[-1:-2]
